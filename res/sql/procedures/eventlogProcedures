
create procedure event_get(in gameid int)
begin
  select event_text from eventlog where gameid = game_id order by time_in asc limit 1;
end;


create procedure event_add(in gameid int, in message varchar(40))
begin
  insert into eventlog(game_id, time_in, event_text) values (gameid, now(), message);
end;